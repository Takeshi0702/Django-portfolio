・my_portfolioはアプリケーション名

・「mysite」のsettings.pyのINSTALLED_APPSに「'widget_tweaks',」と「my_portfolio」を追加する。
「'widget_tweaks',」はホームをカスタマイズする時に便利、またテンプレートでフォームをレンダリングする時にcssクラスや属性を変更できるモジュールです。後で必要となるので先に追加をしておく。


【トップページから作成する。】
《大切》
「models.py」を一番最初に作成する。
models.py = データベースの中身

・「models」を作成する。modelsを使用する事でSQLを意識する事なくデータベースを操作できる。
modelを定義するためにmodels.pyに追加する

『profilemodelの作成』
・最初にProfileのclassを作成する。
class Profile(models.Model):
    title = 

